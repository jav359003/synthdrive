services:
  - type: web
    name: synthdrive-detection
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn detection_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: "10000"
